services:
  - type: web
    name: traffic-flow-backend
    runtime: docker
    dockerfilePath: ./Backend/Dockerfile
    dockerContext: .
    plan: free
    autoDeploy: true
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 10000
      - key: FRONTEND_DIR
        value: /app/Frontend
      - key: CACHE_NAMESPACE
        value: model-v2
      - key: ML_HOSTPORT
        fromService:
          type: pserv
          name: traffic-flow-prediction-service
          property: hostport
      - key: REDIS_URL
        fromDatabase:
          name: traffic-flow-cache
          property: connectionString

  - type: pserv
    name: traffic-flow-prediction-service
    runtime: docker
    dockerfilePath: ./prediction-service/Dockerfile
    dockerContext: ./prediction-service
    plan: starter
    autoDeploy: true

  - type: keyvalue
    name: traffic-flow-cache
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []
